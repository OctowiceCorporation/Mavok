<!-- Recently Viewed -->
<div class="viewed">
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="viewed_title_container">
                    <h3 class="viewed_title">Recently Viewed</h3>

                </div>

                <div class="viewed_slider_container">

                    <!-- Recently Viewed Slider -->

                    <div class="scrolling-wrapper">
                        <!-- Recently Viewed Item -->
                        {% for product in products %}
                        <div class="product_item is_new line_item ml-3" style="width: 22%;">
                            <a href="{{ path('product', {'slug': product.slug}) }}"></a>
                                <div class="product_border"></div>
                                <div class="product_image d-flex flex-column align-items-center justify-content-center"><img style="object-fit: contain; width: 100px; height: 100px" src="/images/{{ product.image }}" alt=""></div>
                                <div class="product_content" style="text-align: center">
                                    <div class="product_price">
                                        {% if product.ProductValue is not null %}
                                        {{ (product.retailPrice * product.ProductValue) }} UAH / {{ product.retailPrice }} {{ product.currencyName }}</div>
                                    {% else %}
                                    {{ product.retailPrice }} UAH</div>
                                {% endif %}
                                <div style="width : 150px; overflow:hidden; display:inline-block; text-overflow: ellipsis; white-space: nowrap;" class="product_name"><div><a href="#" tabindex="0">{{ product.name }}</a></div></div>
                        </div>
                        <div class="button_container" style="text-align: center">
                            <button type="button" class="button cart_button add_to_basket text-light" value="{{ product.slug }}">Add to Cart</button>
                        </div>

                        <ul class="product_marks">
                            {% if product.sale is not null %}
                                <li class="product_mark product_discount">-{{ product.sale }}%</li>
                            {% endif %}
                            {% if product.specialOffer == 1 %}
                                <li class="product_mark product_new">Хит продаж</li>
                            {% endif %}
                        </ul>
                        </a>
                    </div>
                    {% endfor %}

                    </div>
                 </div>
            </div>
         </div>
    </div>
</div>